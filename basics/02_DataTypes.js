"use strict"; // treat all code as newer version of JavaScript, but it will not work in older browsers.

// alert("Junaid Mehmood"); // I am using node.js, so I cannot use alert, but in a browser environment, this would display an alert box with the message "Junaid Mehmood".

let name = "Junaid Mehmood" // This is string datatype
let age = 20 // This is int data type.
let isLoggedIn = true // This is boolean datatype.

// number can hold upto 2^53 numbers
// We can use bigint if we require more numbers and that is used in stock market.
// string is written in '' or "".
// boolean can be true or false.
// null is a standalone value.
// undefined means value is not yet assigned.
// symbol is used for uniqueness of the variables.
// object 
// typeof tells us the datatype of particular variable.

console.log(typeof isLoggedIn);